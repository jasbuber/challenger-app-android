<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.cespenar.thechallenger.ChallengeParticipationsActivity"
    style="@style/CommonScreen">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/discover_challenges"
        style="@style/CommonButton"
        android:background="@drawable/button_search"
        android:visibility="gone"
        android:id="@+id/join_first_challenge_action"
        android:onClick="onClickJoinFirstChallenge"/>

    <ListView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/challenge_participations_list"
        android:dividerHeight="10sp"
        android:divider="@color/background_dark_blue"/>

    <ProgressBar
        android:id="@+id/my_participations_loader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:layout_centerInParent="true"
        android:visibility="gone"/>

</RelativeLayout>
